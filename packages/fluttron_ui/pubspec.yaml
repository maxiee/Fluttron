name: fluttron_ui
description: "Fluttron UI framework — Flutter Web widgets, service clients, and bridge utilities."
publish_to: 'none'

version: 0.1.0-alpha

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  fluttron_shared:
    path: ../fluttron_shared
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
